name: 'warehouse'
version: '1.0.0'
config-version: 2
profile: 'postgres'
model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
target-path: "target"

models:
  warehouse:
    staging:
      github:
        stg_github_pulls:
          +tags: ["pulls"]
        stg_github_contributions:
          +tags: ["contributions"]
  elementary:
    +schema: elementary